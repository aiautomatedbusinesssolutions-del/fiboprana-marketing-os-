# X pattern log — mechanisms, not topics

> When a post clearly works, log the MECHANISM here (the shape that made it
> work), never the topic. The weekly X batch generator reads this file and
> reuses confirmed mechanisms on fresh material.
>
> How entries get here: during reply/close sessions, Claude proposes a row
> when a post's numbers stand out (impressions vs the batch's median; data
> lives in metric_snapshots x_post rows); the founder confirms or deletes.
> Mark new entries `(provisional)` until a second post repeats the win.
> Keep this file SHORT — a pattern that stops working gets removed, not
> argued with.

## Confirmed

- (none yet — the Fiboprana X account has no post data)

## Provisional

- (none yet)
